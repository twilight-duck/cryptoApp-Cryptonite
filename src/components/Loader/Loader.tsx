import { FC } from 'react';
import { Spin } from 'antd';
import './Loader.scss';

interface ILoader {
}

export const Loader: FC<ILoader> = () => {
    return (
        <div className="loader">
            
        </div>
    )
};
